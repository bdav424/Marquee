<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:label="Winchester Marquee"
        android:icon="@drawable/ic_marquee"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- The board runs on a box on the LAN over plain HTTP, so cleartext
             is allowed for that host only rather than app-wide. -->
        <receiver
            android:name=".MarqueeWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.winchester.marquee.REFRESH" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/marquee_widget_info" />
        </receiver>
    </application>
</manifest>
